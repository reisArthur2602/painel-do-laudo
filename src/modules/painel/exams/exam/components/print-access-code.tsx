import { PrinterIcon } from 'lucide-react';
import { Button } from '../../../../../components/ui/button';

export const PrintAccessCode = () => {
    return (
        <Button variant="secondary">
            <PrinterIcon /> Código de Acesso
        </Button>
    );
};
